# Shared Packages

Utilitários compartilhados por domínios e apps.

Este pacote deve receber apenas código sem dependência de runtime HTTP, como paginação, mappers, validação comum, ObjectId e resolução de provider de banco.
