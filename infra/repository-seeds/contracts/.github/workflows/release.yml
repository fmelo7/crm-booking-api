name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    name: Validate and publish contracts release
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Validate contracts
        run: npm run test:contracts

      - name: Build
        run: npm run build

      - name: Publish hook
        if: ${{ secrets.RELEASE_WEBHOOK_URL != '' }}
        run: |
          curl -fsS -X POST "${{ secrets.RELEASE_WEBHOOK_URL }}" \
            -H "authorization: Bearer ${{ secrets.RELEASE_WEBHOOK_TOKEN }}" \
            -H "content-type: application/json" \
            -d '{"package":"contracts","ref":"${{ github.ref_name }}","sha":"${{ github.sha }}"}'
