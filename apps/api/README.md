# API app

Entrypoint do runtime HTTP principal.

Por enquanto ele delega para `src/`, mantendo a migração incremental. Quando o projeto for fisicamente reorganizado, este diretório deve receber o bootstrap NestJS, módulos HTTP e configuração de deploy da API.
