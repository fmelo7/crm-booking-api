# Frontend app

Aplicação web do CRM.

Hoje o frontend é estático e fica em `apps/frontend/public`, servido pela API. Este diretório já isola o ponto de entrada para uma futura migração para React/Vite ou outro build dedicado.
